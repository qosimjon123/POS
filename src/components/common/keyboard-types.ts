/** Internal layout for simple-keyboard (maps from public `type` prop). */
export type KeyboardLayoutMode = 'full' | 'numeric';

export type KeyboardTypeProp = 'full' | 'number';

export type KeyboardPosition = 'floating' | 'embedded';
